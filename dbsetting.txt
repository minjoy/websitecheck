# 데이터베이스 세팅 가이드
# AI 특가 마켓플레이스 - MySQL on AWS RDS

## 목차
1. AWS RDS MySQL 인스턴스 생성
2. 데이터베이스 및 테이블 생성
3. 환경 변수 설정
4. 데이터 마이그레이션 (LocalStorage → MySQL)

---

## 1. AWS RDS MySQL 인스턴스 생성

### 1.1 AWS RDS 콘솔 접속
1. AWS Management Console에 로그인
2. RDS 서비스로 이동
3. "데이터베이스 생성" 클릭

### 1.2 데이터베이스 설정
```
엔진 옵션: MySQL
에디션: MySQL Community
버전: MySQL 8.0.x (최신 안정 버전)

템플릿:
- 프로덕션: 프로덕션 환경 (고가용성 필요시)
- 개발/테스트: 개발 및 테스트 환경 (권장)
- 프리 티어: 무료 체험

설정:
- DB 인스턴스 식별자: marketplace-deals-db
- 마스터 사용자 이름: admin (또는 원하는 이름)
- 마스터 암호: [강력한 비밀번호 설정]

DB 인스턴스 클래스:
- 프로덕션: db.t3.small 이상
- 개발/테스트: db.t3.micro (프리 티어 해당)

스토리지:
- 스토리지 유형: 범용 SSD (gp3)
- 할당된 스토리지: 20 GB
- 스토리지 자동 조정: 활성화 (최대 100 GB)

연결:
- 컴퓨팅 리소스: EC2 컴퓨팅 리소스에 연결 안 함 (공개 액세스 필요시)
- 퍼블릭 액세스: 예 (개발 환경) / 아니오 (프로덕션 환경)
- VPC 보안 그룹: 새로 생성 또는 기존 그룹 선택
- 데이터베이스 포트: 3306

추가 구성:
- 초기 데이터베이스 이름: marketplace_deals
- DB 파라미터 그룹: default.mysql8.0
- 백업 보존 기간: 7일
- 암호화: 활성화
```

### 1.3 보안 그룹 설정
RDS 보안 그룹에서 인바운드 규칙 추가:
```
유형: MySQL/Aurora (3306)
소스:
  - 개발 환경: 내 IP 주소 (xxx.xxx.xxx.xxx/32)
  - 프로덕션: EC2 보안 그룹 ID 또는 특정 IP 대역
```

---

## 2. 데이터베이스 및 테이블 생성

### 2.1 MySQL 접속
```bash
# 로컬에서 RDS 접속
mysql -h [RDS엔드포인트] -P 3306 -u admin -p

# 예시:
mysql -h marketplace-deals-db.xxxxxxxxxx.ap-northeast-2.rds.amazonaws.com -P 3306 -u admin -p
```

### 2.2 데이터베이스 생성 (이미 생성되었다면 생략)
```sql
CREATE DATABASE IF NOT EXISTS marketplace_deals
CHARACTER SET utf8mb4
COLLATE utf8mb4_unicode_ci;

USE marketplace_deals;
```

### 2.3 테이블 생성 스크립트

#### Users 테이블
```sql
CREATE TABLE users (
  id VARCHAR(36) PRIMARY KEY,
  email VARCHAR(255) UNIQUE NOT NULL,
  name VARCHAR(100) NOT NULL,
  role ENUM('admin', 'user') DEFAULT 'user',
  password VARCHAR(255) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_email (email),
  INDEX idx_role (role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### Products 테이블
```sql
CREATE TABLE products (
  id VARCHAR(36) PRIMARY KEY,
  title VARCHAR(500) NOT NULL,
  original_price DECIMAL(12, 2) NOT NULL,
  sale_price DECIMAL(12, 2) NOT NULL,
  discount_rate TINYINT UNSIGNED NOT NULL,
  image_url TEXT,
  marketplace ENUM('coupang', 'naver', 'gmarket', '11st', 'instagram', 'blog', 'cafe', 'other') NOT NULL,
  product_url TEXT NOT NULL,
  rating DECIMAL(3, 2) DEFAULT 0.00,
  review_count INT UNSIGNED DEFAULT 0,
  deal_date DATE NOT NULL,
  deal_end_date DATE NOT NULL,
  category VARCHAR(100),
  tags JSON,
  is_verified BOOLEAN DEFAULT FALSE,
  author_id VARCHAR(36) NOT NULL,
  author_name VARCHAR(100) NOT NULL,
  type ENUM('deal', 'group-buy') NOT NULL,
  partners_text TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (author_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_author (author_id),
  INDEX idx_type (type),
  INDEX idx_marketplace (marketplace),
  INDEX idx_deal_date (deal_date),
  INDEX idx_discount (discount_rate),
  INDEX idx_verified (is_verified),
  INDEX idx_created (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### Review Summaries 테이블
```sql
CREATE TABLE review_summaries (
  id INT AUTO_INCREMENT PRIMARY KEY,
  product_id VARCHAR(36) UNIQUE NOT NULL,
  overall_sentiment ENUM('positive', 'neutral', 'negative') NOT NULL,
  pros JSON,
  cons JSON,
  key_points JSON,
  ai_insight TEXT,
  total_reviews INT UNSIGNED DEFAULT 0,
  average_rating DECIMAL(3, 2) DEFAULT 0.00,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
  INDEX idx_product (product_id),
  INDEX idx_sentiment (overall_sentiment)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### Favorites 테이블
```sql
CREATE TABLE favorites (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id VARCHAR(36) NOT NULL,
  product_id VARCHAR(36) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
  UNIQUE KEY unique_favorite (user_id, product_id),
  INDEX idx_user (user_id),
  INDEX idx_product (product_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### Keyword Alerts 테이블
```sql
CREATE TABLE keyword_alerts (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id VARCHAR(36) NOT NULL,
  keyword VARCHAR(100) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  UNIQUE KEY unique_keyword_alert (user_id, keyword),
  INDEX idx_user (user_id),
  INDEX idx_keyword (keyword)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### Blacklist 테이블
```sql
CREATE TABLE blacklist (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id VARCHAR(36) UNIQUE NOT NULL,
  email VARCHAR(255) NOT NULL,
  name VARCHAR(100) NOT NULL,
  reason TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 2.4 초기 관리자 계정 생성
```sql
-- 비밀번호는 bcrypt로 해싱된 값을 사용해야 함
-- 예시: 'admin123' → bcrypt 해시
INSERT INTO users (id, email, name, role, password, created_at)
VALUES (
  UUID(),
  'admin@example.com',
  'Admin',
  'admin',
  '$2a$10$...[bcrypt_해시된_비밀번호]',
  NOW()
);
```

---

## 3. 환경 변수 설정

### 3.1 .env.local 파일 생성
프로젝트 루트에 `.env.local` 파일 생성:

```env
# Database Configuration
DB_HOST=marketplace-deals-db.xxxxxxxxxx.ap-northeast-2.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=[your_password]
DB_NAME=marketplace_deals

# Database Pool Configuration
DB_CONNECTION_LIMIT=10
DB_QUEUE_LIMIT=0

# SSL Configuration (프로덕션 환경)
DB_SSL=true

# API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
```

### 3.2 Next.js API 설정
`lib/db.ts` 파일 생성:

```typescript
import mysql from 'mysql2/promise';

const pool = mysql.createPool({
  host: process.env.DB_HOST,
  port: parseInt(process.env.DB_PORT || '3306'),
  user: process.env.DB_USER,
  password: process.env.DB_PASSWORD,
  database: process.env.DB_NAME,
  waitForConnections: true,
  connectionLimit: parseInt(process.env.DB_CONNECTION_LIMIT || '10'),
  queueLimit: parseInt(process.env.DB_QUEUE_LIMIT || '0'),
  ssl: process.env.DB_SSL === 'true' ? { rejectUnauthorized: true } : undefined,
});

export default pool;
```

---

## 4. 데이터 마이그레이션 (LocalStorage → MySQL)

### 4.1 마이그레이션 스크립트 준비
현재 LocalStorage에 저장된 데이터를 MySQL로 마이그레이션하는 스크립트 작성

### 4.2 순서
1. Users 데이터 마이그레이션
2. Products 데이터 마이그레이션
3. Review Summaries 마이그레이션
4. Favorites 마이그레이션
5. Keyword Alerts 마이그레이션
6. Blacklist 마이그레이션

---

## 5. 성능 최적화

### 5.1 인덱스 최적화
자주 조회되는 필드에 대한 복합 인덱스 생성:

```sql
-- 특가 목록 조회 최적화
CREATE INDEX idx_deal_lookup ON products(deal_date, type, is_verified);

-- 검색 최적화
CREATE FULLTEXT INDEX idx_title_search ON products(title);
CREATE FULLTEXT INDEX idx_category_search ON products(category);
```

### 5.2 쿼리 캐싱
RDS 파라미터 그룹에서 쿼리 캐시 설정:
```
query_cache_type = 1
query_cache_size = 67108864  # 64MB
```

### 5.3 Connection Pooling
애플리케이션에서 connection pool 사용 (위 lib/db.ts 참조)

---

## 6. 백업 및 복구

### 6.1 자동 백업
RDS 콘솔에서 자동 백업 설정:
- 백업 보존 기간: 7일
- 백업 시간대: 트래픽이 적은 시간 (예: 오전 3-4시)

### 6.2 수동 스냅샷
중요한 변경 전 수동 스냅샷 생성:
```
RDS 콘솔 → 데이터베이스 선택 → 작업 → 스냅샷 생성
```

### 6.3 복구
```
RDS 콘솔 → 스냅샷 선택 → 작업 → 스냅샷 복원
```

---

## 7. 모니터링

### 7.1 CloudWatch 메트릭
- CPU 사용률
- 데이터베이스 연결 수
- 읽기/쓰기 IOPS
- 스토리지 사용량

### 7.2 로그 활성화
- Error log
- Slow query log (1초 이상 쿼리)
- General log (개발 환경에서만)

---

## 8. 보안 체크리스트

- [x] RDS 인스턴스가 VPC 내에 있는지 확인
- [x] 보안 그룹에서 필요한 IP만 허용
- [x] 마스터 비밀번호는 강력하게 설정
- [x] SSL/TLS 연결 사용
- [x] 정기적인 보안 패치 적용
- [x] IAM 데이터베이스 인증 고려 (고급)
- [x] 암호화된 백업 활성화
- [x] 데이터베이스 감사 로그 활성화

---

## 9. 문제 해결

### 9.1 연결 실패
```bash
# 연결 테스트
telnet [RDS엔드포인트] 3306

# 보안 그룹 확인
# EC2 보안 그룹 또는 IP 화이트리스트 확인
```

### 9.2 성능 저하
```sql
-- Slow query 확인
SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 10;

-- 현재 프로세스 확인
SHOW FULL PROCESSLIST;

-- 인덱스 사용 확인
EXPLAIN [your_query];
```

### 9.3 스토리지 부족
```
RDS 콘솔 → 수정 → 할당된 스토리지 증가
```

---

## 10. 다음 단계

1. API 엔드포인트 구현 (api.txt 참조)
2. 프론트엔드 LocalStorage 코드를 API 호출로 변경
3. 인증/인가 시스템 강화 (JWT, Session 등)
4. 프로덕션 배포 준비

---

## 참고 자료

- AWS RDS MySQL 문서: https://docs.aws.amazon.com/rds/
- MySQL 8.0 공식 문서: https://dev.mysql.com/doc/refman/8.0/
- Next.js API Routes: https://nextjs.org/docs/app/building-your-application/routing/route-handlers
