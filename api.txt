# API 연결 가이드
# AI 특가 마켓플레이스 - RESTful API 설계

## 목차
1. API 아키텍처 개요
2. API 엔드포인트 목록
3. 인증 및 권한
4. 각 엔드포인트 상세 스펙
5. 에러 처리
6. 프론트엔드 연동 가이드

---

## 1. API 아키텍처 개요

### 1.1 기술 스택
- Framework: Next.js 15 App Router
- API: Next.js Route Handlers (app/api/*)
- Database: MySQL (AWS RDS)
- ORM/Query Builder: mysql2
- 인증: JWT (jsonwebtoken) + HTTP-only Cookies
- 검증: zod

### 1.2 디렉토리 구조
```
app/
├── api/
│   ├── auth/
│   │   ├── login/route.ts
│   │   ├── signup/route.ts
│   │   ├── logout/route.ts
│   │   └── me/route.ts
│   ├── products/
│   │   ├── route.ts              # GET (목록), POST (생성)
│   │   ├── [id]/route.ts         # GET, PUT, DELETE
│   │   └── search/route.ts
│   ├── reviews/
│   │   ├── [productId]/route.ts
│   │   └── summary/route.ts
│   ├── favorites/
│   │   ├── route.ts              # GET, POST
│   │   └── [productId]/route.ts  # DELETE
│   ├── keywords/
│   │   ├── route.ts              # GET, POST
│   │   └── [keyword]/route.ts    # DELETE
│   └── admin/
│       ├── blacklist/route.ts
│       └── users/route.ts
lib/
├── db.ts                         # Database connection pool
├── auth.ts                       # JWT utilities
└── validators/                   # Zod schemas
```

---

## 2. API 엔드포인트 목록

### 2.1 인증 (Authentication)
```
POST   /api/auth/signup          회원가입
POST   /api/auth/login           로그인
POST   /api/auth/logout          로그아웃
GET    /api/auth/me              현재 사용자 정보
PUT    /api/auth/me              사용자 정보 수정
DELETE /api/auth/me              회원 탈퇴
```

### 2.2 상품 (Products)
```
GET    /api/products             상품 목록 조회 (필터링, 페이지네이션)
POST   /api/products             상품 등록 (인증 필요)
GET    /api/products/:id         상품 상세 조회
PUT    /api/products/:id         상품 수정 (작성자 또는 관리자)
DELETE /api/products/:id         상품 삭제 (작성자 또는 관리자)
GET    /api/products/search      상품 검색
```

### 2.3 리뷰 요약 (Review Summaries)
```
GET    /api/reviews/:productId   특정 상품의 리뷰 요약
POST   /api/reviews              리뷰 요약 생성 (관리자 전용)
PUT    /api/reviews/:productId   리뷰 요약 수정 (관리자 전용)
```

### 2.4 즐겨찾기 (Favorites)
```
GET    /api/favorites            사용자의 즐겨찾기 목록
POST   /api/favorites            즐겨찾기 추가
DELETE /api/favorites/:productId 즐겨찾기 제거
```

### 2.5 키워드 알림 (Keyword Alerts)
```
GET    /api/keywords             사용자의 키워드 목록
POST   /api/keywords             키워드 추가
DELETE /api/keywords/:keyword    키워드 삭제
```

### 2.6 관리자 (Admin)
```
GET    /api/admin/blacklist      블랙리스트 목록
POST   /api/admin/blacklist      블랙리스트 추가
DELETE /api/admin/blacklist/:userId 블랙리스트 해제
GET    /api/admin/users          사용자 목록
```

---

## 3. 인증 및 권한

### 3.1 JWT 구현

#### lib/auth.ts
```typescript
import jwt from 'jsonwebtoken';
import { cookies } from 'next/headers';

const JWT_SECRET = process.env.JWT_SECRET || 'your-secret-key';
const JWT_EXPIRES_IN = '7d';

export interface JWTPayload {
  userId: string;
  email: string;
  role: 'admin' | 'user';
}

export function generateToken(payload: JWTPayload): string {
  return jwt.sign(payload, JWT_SECRET, { expiresIn: JWT_EXPIRES_IN });
}

export function verifyToken(token: string): JWTPayload | null {
  try {
    return jwt.verify(token, JWT_SECRET) as JWTPayload;
  } catch {
    return null;
  }
}

export async function getCurrentUser(): Promise<JWTPayload | null> {
  const cookieStore = await cookies();
  const token = cookieStore.get('auth_token')?.value;

  if (!token) return null;

  return verifyToken(token);
}

export function setAuthCookie(token: string) {
  const cookieStore = cookies();
  cookieStore.set('auth_token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 60 * 60 * 24 * 7, // 7 days
    path: '/',
  });
}

export function clearAuthCookie() {
  const cookieStore = cookies();
  cookieStore.delete('auth_token');
}
```

### 3.2 인증 미들웨어
```typescript
// lib/middleware/auth.ts
import { NextRequest, NextResponse } from 'next/server';
import { getCurrentUser } from '@/lib/auth';

export async function requireAuth(request: NextRequest) {
  const user = await getCurrentUser();

  if (!user) {
    return NextResponse.json(
      { error: '인증이 필요합니다' },
      { status: 401 }
    );
  }

  return user;
}

export async function requireAdmin(request: NextRequest) {
  const user = await getCurrentUser();

  if (!user || user.role !== 'admin') {
    return NextResponse.json(
      { error: '관리자 권한이 필요합니다' },
      { status: 403 }
    );
  }

  return user;
}
```

---

## 4. 각 엔드포인트 상세 스펙

### 4.1 인증 API

#### POST /api/auth/signup
회원가입

**Request:**
```json
{
  "email": "user@example.com",
  "password": "password123",
  "name": "홍길동"
}
```

**Response (201):**
```json
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "role": "user",
    "createdAt": "2025-01-01T00:00:00.000Z"
  },
  "token": "jwt_token_here"
}
```

**Errors:**
- 400: 이메일 중복, 유효성 검증 실패
- 500: 서버 에러

#### POST /api/auth/login
로그인

**Request:**
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

**Response (200):**
```json
{
  "user": {
    "id": "uuid",
    "email": "user@example.com",
    "name": "홍길동",
    "role": "user"
  },
  "token": "jwt_token_here"
}
```

**Errors:**
- 401: 이메일 또는 비밀번호 불일치
- 500: 서버 에러

#### GET /api/auth/me
현재 사용자 정보 조회 (인증 필요)

**Response (200):**
```json
{
  "id": "uuid",
  "email": "user@example.com",
  "name": "홍길동",
  "role": "user",
  "createdAt": "2025-01-01T00:00:00.000Z"
}
```

**Errors:**
- 401: 인증되지 않음
- 500: 서버 에러

---

### 4.2 상품 API

#### GET /api/products
상품 목록 조회

**Query Parameters:**
```
?page=1              # 페이지 번호 (기본값: 1)
&limit=20            # 페이지당 항목 수 (기본값: 20, 최대: 100)
&type=deal           # 상품 타입 (deal, group-buy)
&marketplace=coupang # 마켓플레이스 필터
&category=전자기기   # 카테고리 필터
&isVerified=true     # 검증 상품만
&dealDate=2025-01-01 # 특정 날짜 특가
&sortBy=created_at   # 정렬 기준 (created_at, discount_rate, sale_price)
&order=desc          # 정렬 순서 (asc, desc)
```

**Response (200):**
```json
{
  "products": [
    {
      "id": "uuid",
      "title": "상품명",
      "originalPrice": 100000,
      "salePrice": 60000,
      "discountRate": 40,
      "imageUrl": "https://...",
      "marketplace": "coupang",
      "productUrl": "https://...",
      "rating": 4.8,
      "reviewCount": 1234,
      "dealDate": "2025-01-01",
      "dealEndDate": "2025-01-07",
      "category": "전자기기",
      "tags": ["특가", "인기"],
      "isVerified": true,
      "authorId": "uuid",
      "authorName": "작성자",
      "type": "deal",
      "partnersText": "파트너스 활동...",
      "createdAt": "2025-01-01T00:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 150,
    "totalPages": 8
  }
}
```

#### POST /api/products
상품 등록 (인증 필요)

**Request:**
```json
{
  "title": "상품명",
  "originalPrice": 100000,
  "salePrice": 60000,
  "imageUrl": "https://...",
  "marketplace": "coupang",
  "productUrl": "https://...",
  "dealDate": "2025-01-01",
  "dealEndDate": "2025-01-07",
  "category": "전자기기",
  "partnersText": "파트너스 활동으로 인해...",
  "rating": 4.8,
  "reviewCount": 1234
}
```

**Response (201):**
```json
{
  "product": {
    "id": "uuid",
    "title": "상품명",
    ...
  }
}
```

**Errors:**
- 401: 인증되지 않음
- 403: 블랙리스트 사용자
- 400: 유효성 검증 실패
- 500: 서버 에러

#### GET /api/products/:id
상품 상세 조회

**Response (200):**
```json
{
  "id": "uuid",
  "title": "상품명",
  ...
}
```

**Errors:**
- 404: 상품을 찾을 수 없음
- 500: 서버 에러

#### PUT /api/products/:id
상품 수정 (작성자 또는 관리자)

**Request:** POST와 동일

**Response (200):**
```json
{
  "product": { ... }
}
```

**Errors:**
- 401: 인증되지 않음
- 403: 권한 없음
- 404: 상품을 찾을 수 없음
- 500: 서버 에러

#### DELETE /api/products/:id
상품 삭제 (작성자 또는 관리자)

**Response (200):**
```json
{
  "message": "상품이 삭제되었습니다"
}
```

---

### 4.3 리뷰 요약 API

#### GET /api/reviews/:productId
특정 상품의 리뷰 요약 조회

**Response (200):**
```json
{
  "productId": "uuid",
  "overallSentiment": "positive",
  "pros": ["장점1", "장점2"],
  "cons": ["단점1", "단점2"],
  "keyPoints": ["핵심1", "핵심2"],
  "aiInsight": "AI 분석 결과...",
  "totalReviews": 1234,
  "averageRating": 4.8
}
```

**Errors:**
- 404: 리뷰 요약을 찾을 수 없음
- 500: 서버 에러

---

### 4.4 즐겨찾기 API

#### GET /api/favorites
사용자의 즐겨찾기 목록 (인증 필요)

**Response (200):**
```json
{
  "favorites": [
    {
      "id": 123,
      "productId": "uuid",
      "product": { ... },
      "createdAt": "2025-01-01T00:00:00.000Z"
    }
  ]
}
```

#### POST /api/favorites
즐겨찾기 추가 (인증 필요)

**Request:**
```json
{
  "productId": "uuid"
}
```

**Response (201):**
```json
{
  "id": 123,
  "productId": "uuid",
  "createdAt": "2025-01-01T00:00:00.000Z"
}
```

**Errors:**
- 400: 이미 즐겨찾기에 추가됨
- 404: 상품을 찾을 수 없음

#### DELETE /api/favorites/:productId
즐겨찾기 제거 (인증 필요)

**Response (200):**
```json
{
  "message": "즐겨찾기가 제거되었습니다"
}
```

---

### 4.5 키워드 알림 API

#### GET /api/keywords
사용자의 키워드 목록 (인증 필요)

**Response (200):**
```json
{
  "keywords": ["노트북", "에어팟", "스마트폰"]
}
```

#### POST /api/keywords
키워드 추가 (인증 필요)

**Request:**
```json
{
  "keyword": "노트북"
}
```

**Response (201):**
```json
{
  "id": 123,
  "keyword": "노트북",
  "createdAt": "2025-01-01T00:00:00.000Z"
}
```

**Errors:**
- 400: 이미 등록된 키워드

#### DELETE /api/keywords/:keyword
키워드 삭제 (인증 필요)

**Response (200):**
```json
{
  "message": "키워드가 삭제되었습니다"
}
```

---

### 4.6 관리자 API

#### GET /api/admin/blacklist
블랙리스트 목록 (관리자 전용)

**Response (200):**
```json
{
  "blacklist": [
    {
      "id": 1,
      "userId": "uuid",
      "email": "user@example.com",
      "name": "홍길동",
      "reason": "스팸",
      "createdAt": "2025-01-01T00:00:00.000Z"
    }
  ]
}
```

#### POST /api/admin/blacklist
블랙리스트 추가 (관리자 전용)

**Request:**
```json
{
  "userId": "uuid",
  "reason": "스팸"
}
```

**Response (201):**
```json
{
  "id": 1,
  "userId": "uuid",
  "reason": "스팸",
  "createdAt": "2025-01-01T00:00:00.000Z"
}
```

---

## 5. 에러 처리

### 5.1 에러 응답 형식
```json
{
  "error": "에러 메시지",
  "code": "ERROR_CODE",
  "details": { ... }
}
```

### 5.2 HTTP 상태 코드
```
200 - OK: 성공
201 - Created: 리소스 생성 성공
204 - No Content: 성공 (응답 본문 없음)
400 - Bad Request: 잘못된 요청
401 - Unauthorized: 인증되지 않음
403 - Forbidden: 권한 없음
404 - Not Found: 리소스를 찾을 수 없음
409 - Conflict: 중복 리소스
422 - Unprocessable Entity: 유효성 검증 실패
429 - Too Many Requests: 요청 제한 초과
500 - Internal Server Error: 서버 에러
503 - Service Unavailable: 서비스 이용 불가
```

---

## 6. 프론트엔드 연동 가이드

### 6.1 API 클라이언트 설정

#### lib/apiClient.ts
```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3000/api';

export class ApiClient {
  private async request<T>(
    endpoint: string,
    options: RequestInit = {}
  ): Promise<T> {
    const url = `${API_URL}${endpoint}`;

    const response = await fetch(url, {
      ...options,
      headers: {
        'Content-Type': 'application/json',
        ...options.headers,
      },
      credentials: 'include', // 쿠키 포함
    });

    if (!response.ok) {
      const error = await response.json();
      throw new Error(error.error || 'API 요청 실패');
    }

    return response.json();
  }

  async get<T>(endpoint: string): Promise<T> {
    return this.request<T>(endpoint, { method: 'GET' });
  }

  async post<T>(endpoint: string, data: any): Promise<T> {
    return this.request<T>(endpoint, {
      method: 'POST',
      body: JSON.stringify(data),
    });
  }

  async put<T>(endpoint: string, data: any): Promise<T> {
    return this.request<T>(endpoint, {
      method: 'PUT',
      body: JSON.stringify(data),
    });
  }

  async delete<T>(endpoint: string): Promise<T> {
    return this.request<T>(endpoint, { method: 'DELETE' });
  }
}

export const api = new ApiClient();
```

### 6.2 서비스 레이어

#### lib/services/authService.ts
```typescript
import { api } from '@/lib/apiClient';
import { User } from '@/lib/types';

export const authService = {
  async signup(email: string, password: string, name: string) {
    return api.post<{ user: User; token: string }>('/auth/signup', {
      email,
      password,
      name,
    });
  },

  async login(email: string, password: string) {
    return api.post<{ user: User; token: string }>('/auth/login', {
      email,
      password,
    });
  },

  async logout() {
    return api.post('/auth/logout', {});
  },

  async getCurrentUser() {
    return api.get<User>('/auth/me');
  },

  async deleteAccount() {
    return api.delete('/auth/me');
  },
};
```

#### lib/services/productService.ts
```typescript
import { api } from '@/lib/apiClient';
import { Product } from '@/lib/types';

export const productService = {
  async getProducts(params: {
    page?: number;
    limit?: number;
    type?: string;
    marketplace?: string;
    category?: string;
    isVerified?: boolean;
    dealDate?: string;
  }) {
    const query = new URLSearchParams(
      Object.entries(params)
        .filter(([_, v]) => v !== undefined)
        .map(([k, v]) => [k, String(v)])
    );

    return api.get<{
      products: Product[];
      pagination: {
        page: number;
        limit: number;
        total: number;
        totalPages: number;
      };
    }>(`/products?${query}`);
  },

  async getProduct(id: string) {
    return api.get<Product>(`/products/${id}`);
  },

  async createProduct(data: Partial<Product>) {
    return api.post<{ product: Product }>('/products', data);
  },

  async updateProduct(id: string, data: Partial<Product>) {
    return api.put<{ product: Product }>(`/products/${id}`, data);
  },

  async deleteProduct(id: string) {
    return api.delete(`/products/${id}`);
  },
};
```

### 6.3 React Query 사용 (권장)

#### lib/hooks/useProducts.ts
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import { productService } from '@/lib/services/productService';

export function useProducts(params: any) {
  return useQuery({
    queryKey: ['products', params],
    queryFn: () => productService.getProducts(params),
  });
}

export function useProduct(id: string) {
  return useQuery({
    queryKey: ['product', id],
    queryFn: () => productService.getProduct(id),
    enabled: !!id,
  });
}

export function useCreateProduct() {
  const queryClient = useQueryClient();

  return useMutation({
    mutationFn: productService.createProduct,
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['products'] });
    },
  });
}
```

---

## 7. 마이그레이션 체크리스트

### 7.1 LocalStorage → API 변경 항목

1. **인증 (lib/auth.ts)**
   - [x] getCurrentUser() → authService.getCurrentUser()
   - [x] login() → authService.login()
   - [x] signup() → authService.signup()
   - [x] logout() → authService.logout()

2. **상품 (lib/mockData.ts)**
   - [x] getAllProducts() → productService.getProducts()
   - [x] getProductById() → productService.getProduct()
   - [x] addProduct() → productService.createProduct()
   - [x] updateProduct() → productService.updateProduct()
   - [x] deleteProduct() → productService.deleteProduct()

3. **즐겨찾기 (lib/favorites.ts)**
   - [x] getUserFavorites() → favoriteService.getFavorites()
   - [x] toggleFavorite() → favoriteService.add/remove()

4. **키워드 알림 (lib/keywordAlerts.ts)**
   - [x] getUserKeywords() → keywordService.getKeywords()
   - [x] addKeyword() → keywordService.addKeyword()
   - [x] removeKeyword() → keywordService.removeKeyword()

---

## 8. 테스트

### 8.1 API 엔드포인트 테스트 (Postman/Thunder Client)

1. 인증 흐름:
   ```
   POST /api/auth/signup → 회원가입
   POST /api/auth/login → 로그인 (토큰 받기)
   GET /api/auth/me → 사용자 정보 확인
   ```

2. 상품 CRUD:
   ```
   GET /api/products → 목록 조회
   POST /api/products → 상품 등록
   GET /api/products/:id → 상세 조회
   PUT /api/products/:id → 수정
   DELETE /api/products/:id → 삭제
   ```

### 8.2 통합 테스트
```bash
npm run test:api
```

---

## 9. 배포 및 환경 설정

### 9.1 환경 변수 (.env.production)
```env
# Database
DB_HOST=your-rds-endpoint.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=your_password
DB_NAME=marketplace_deals
DB_SSL=true

# JWT
JWT_SECRET=your-very-secure-random-secret-key-here

# API
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# CORS
ALLOWED_ORIGINS=https://your-domain.com
```

### 9.2 배포 체크리스트
- [x] 환경 변수 설정
- [x] DB 마이그레이션 실행
- [x] SSL/TLS 활성화
- [x] CORS 설정
- [x] Rate Limiting 설정
- [x] 에러 로깅 (Sentry 등)
- [x] API 문서화 (Swagger)

---

## 10. 참고 자료

- Next.js Route Handlers: https://nextjs.org/docs/app/building-your-application/routing/route-handlers
- MySQL2 문서: https://github.com/sidorares/node-mysql2
- JWT 문서: https://jwt.io/
- React Query: https://tanstack.com/query/latest
